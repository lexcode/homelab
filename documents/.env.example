# paperless-ngx
# Random secret key — generate with:
# python3 -c "import secrets; print(secrets.token_hex(32))"
PAPERLESS_SECRET_KEY=
TZ=Europe/Dublin
PAPERLESS_DB_USERNAME=
PAPERLESS_DB_PASSWORD=
PAPERLESS_DB_NAME=

# Shared API token for paperless-ai and paperless-gpt — get it after first run:
# docker compose exec paperless-ngx python3 manage.py drf_create_token <your-admin-username>
PAPERLESS_API_TOKEN=

# Shared host settings
PUID=1000
PGID=1000
OLLAMA_HOST=http://192.168.0.x:11434 # LAN IP of the machine running Ollama (not the Pi)

# paperless-ai
PAPERLESS_API_URL=http://paperless-ngx:8000/api
PAPERLESS_USERNAME=lexcode # change to your paperless username
AI_PROVIDER=ollama
OLLAMA_API_URL=${OLLAMA_HOST}
OLLAMA_MODEL=qwen3:8b
RAG_SERVICE_URL=http://paperless-ngx:8000
RAG_SERVICE_ENABLED=true
SCAN_INTERVAL=*/30 * * * *
PAPERLESS_URL=http://paperless-ngx:8000

# paperless-gpt
PAPERLESS_BASE_URL=http://paperless-ngx:8000
PAPERLESS_PUBLIC_URL=http://192.168.0.x:8001

# Optional: required only with compose.google-docai.yml
# GOOGLE_APPLICATION_CREDENTIALS_HOST=/path/to/google-credentials.json
