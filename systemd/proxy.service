[Unit]
Description=Proxy Stack (Cloudflare Tunnel + Nginx Proxy Manager)
After=docker.service media.service monitoring.service analytics.service documents.service terminal.service homepage.service management.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/lexcode/code/homelab/proxy
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
